<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chauthai.swipereveallayoutdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RecyclerDemoActivity"
            android:label="RecyclerView Demo"/>

        <activity android:name=".ListDemoActivity"
            android:label="ListView Demo" />

        <activity android:name=".GridActivity"
            android:label="Grid Demo" />

    </application>

</manifest>
